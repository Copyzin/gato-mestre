name = "gato-mestre-api"
main = "src/index.ts"
compatibility_date = "2024-12-18"

# Variáveis de ambiente locais ficam em .dev.vars (não commitado).
# Em produção, usar: npx wrangler secret put DATABASE_URL

[vars]
ENVIRONMENT = "development"
